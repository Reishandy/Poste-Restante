name: poste-restante

services:
  relay:
    build: .
    restart: unless-stopped
    environment:
      APP_NAME: ${APP_NAME:-poste-restante-relay}
      DEV_MODE: ${DEV_MODE:-false}
    ports:
      - "${APP_PORT:-8000}:8000"